'use client';

import { useState } from 'react';
import { BarChart3, CheckCircle2, DollarSign, LogOut, Plus, Printer, Settings, Truck, Users, X } from 'lucide-react';

const initialBookings = [];
const initialDrivers = [{ id: 1, name: 'T. Klaasen', phone: '078 821 4401', vehicle: 'Bakkie', area: 'South Peninsula', rating: 4.9 }, { id: 2, name: 'S. Jacobs', phone: '072 310 2230', vehicle: '4T Truck', area: 'Cape Town CBD', rating: 4.8 }];

export default function AdminDashboard() {
  const [loggedIn, setLoggedIn] = useState(false); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [loginError, setLoginError] = useState('');
  if (!loggedIn) return <main className="bg-mist"><div className="container flex min-h-[620px] items-center justify-center py-16"><form onSubmit={e => { e.preventDefault(); if (email === 'admin@jrms.co.za' && password === 'admin123') { setLoggedIn(true); setLoginError(''); } else setLoginError('Incorrect admin email or password.'); }} className="w-full max-w-md rounded-2xl bg-white p-8 shadow-soft"><div className="mb-8"><div className="mb-6 flex h-12 w-12 items-center justify-center rounded-xl bg-brand text-white"><BarChart3 /></div><p className="eyebrow mb-2">JRMS operations</p><h1 className="text-3xl font-black tracking-[-.04em]">Admin login.</h1><p className="mt-3 text-sm leading-6 text-slate-500">Manage bookings, driver assignments and platform profit.</p></div><label className="mb-1.5 block text-xs font-bold text-slate-600">Email</label><input required type="email" className="field mb-4" value={email} onChange={e => setEmail(e.target.value)} /><label className="mb-1.5 block text-xs font-bold text-slate-600">Password</label><input required type="password" className="field" value={password} onChange={e => setPassword(e.target.value)} />{loginError && <p className="mt-3 text-sm font-semibold text-red-600">{loginError}</p>}<button className="btn btn-primary mt-6 w-full">Sign in</button><p className="mt-5 text-center text-xs text-slate-400">MVP access: admin@jrms.co.za / admin123</p></form></div></main>;
  return <AdminWorkspace onLogout={() => setLoggedIn(false)} />;
}

function AdminWorkspace({ onLogout }) {
  const [bookings, setBookings] = useState(initialBookings); const [drivers, setDrivers] = useState(initialDrivers); const [showAdd, setShowAdd] = useState(false); const [newDriver, setNewDriver] = useState({ name: '', phone: '', vehicle: '', area: '' });
  const assign = (id, driver) => setBookings(bookings.map(booking => booking.id === id ? { ...booking, driver, status: driver === 'Unassigned' ? 'pending' : 'assigned' } : booking));
  const addDriver = e => { e.preventDefault(); if (!newDriver.name || !newDriver.phone || !newDriver.vehicle || !newDriver.area) return; setDrivers([...drivers, { ...newDriver, id: Date.now(), rating: 5 }]); setNewDriver({ name: '', phone: '', vehicle: '', area: '' }); setShowAdd(false); };
  const revenue = bookings.reduce((sum, booking) => sum + booking.price, 0);
  return <main className="bg-mist"><div className="container py-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="eyebrow mb-2">JRMS operations</p><h1 className="text-4xl font-black tracking-[-.045em]">Admin dashboard</h1><p className="mt-2 text-sm text-slate-500">Saturday, 19 September 2026 · Live booking overview</p></div><div className="flex gap-3"><button onClick={() => window.print()} className="btn btn-outline text-sm"><Printer size={16} /> Print profit sheet</button><button onClick={onLogout} className="btn btn-outline text-sm"><LogOut size={16} /> Log out</button></div></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Metric icon={BarChart3} label="Total bookings" value={bookings.length} detail="No bookings yet" /><Metric icon={DollarSign} label="Total revenue" value={`R${revenue.toLocaleString()}`} detail="Confirmed bookings" /><Metric icon={DollarSign} label="My profit (15%)" value={`R${Math.round(revenue * .15).toLocaleString()}`} detail="Platform share" dark /><Metric icon={Users} label="Active drivers" value={drivers.length} detail="Across Cape Town" /></div><div className="mt-8 grid gap-8 lg:grid-cols-[1.65fr_1fr]"><section className="rounded-2xl bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="text-xl font-black">Bookings</h2><p className="mt-1 text-xs text-slate-500">Assign a driver and track every job.</p></div><span className="rounded-full bg-sky px-3 py-1 text-xs font-bold text-brand">{bookings.length} live</span></div><div className="mt-6 overflow-x-auto">{bookings.length === 0 ? <div className="rounded-xl border border-dashed border-slate-200 px-5 py-12 text-center"><p className="font-bold text-ink">No bookings yet</p><p className="mt-2 text-sm text-slate-500">New WhatsApp quotes will appear here once connected to Supabase.</p></div> : <table className="w-full min-w-[680px] text-left text-sm"><thead className="border-b border-slate-100 text-xs uppercase tracking-wider text-slate-400"><tr><th className="pb-3">Booking</th><th className="pb-3">Client / area</th><th className="pb-3">Price</th><th className="pb-3">Driver</th><th className="pb-3">Profit</th></tr></thead><tbody className="divide-y divide-slate-100">{bookings.map(booking => <tr key={booking.id}><td className="py-4"><p className="font-extrabold text-brand">{booking.id}</p><p className="mt-1 text-xs text-slate-400">{booking.service}</p></td><td className="py-4"><p className="font-bold">{booking.name}</p><p className="mt-1 text-xs text-slate-500">{booking.area}</p></td><td className="py-4 font-bold">R{booking.price}</td><td className="py-4"><select className="field !w-36 !px-2 !py-2 text-xs" value={booking.driver} onChange={e => assign(booking.id, e.target.value)}><option>Unassigned</option>{drivers.map(driver => <option key={driver.id}>{driver.name}</option>)}</select><p className={`mt-1 text-[10px] font-bold uppercase ${booking.status === 'done' ? 'text-green-600' : booking.status === 'assigned' ? 'text-brand' : 'text-amber-600'}`}>{booking.status}</p></td><td className="py-4 font-extrabold text-green-700">R{Math.round(booking.price * .15)}</td></tr>)}</tbody></table>}</div></section><section className="rounded-2xl bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="text-xl font-black">Drivers</h2><p className="mt-1 text-xs text-slate-500">Your current network.</p></div><button onClick={() => setShowAdd(true)} aria-label="Add driver" className="rounded-lg bg-sky p-2 text-brand"><Plus size={19} /></button></div><div className="mt-5 divide-y divide-slate-100">{drivers.map(driver => <div key={driver.id} className="flex items-center justify-between gap-3 py-4"><div><p className="text-sm font-bold">{driver.name}</p><p className="mt-1 text-xs text-slate-500">{driver.vehicle} · {driver.area}</p></div><span className="text-xs font-extrabold text-amber-600">★ {driver.rating}</span></div>)}</div></section></div></div>{showAdd && <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#071b39]/50 p-5"><form onSubmit={addDriver} className="w-full max-w-md rounded-2xl bg-white p-7 shadow-2xl"><div className="flex items-start justify-between"><div><p className="eyebrow mb-2">Driver network</p><h2 className="text-2xl font-black">Add a driver</h2></div><button type="button" onClick={() => setShowAdd(false)} aria-label="Close dialog" className="text-slate-400"><X /></button></div><div className="mt-6 grid gap-4">{[['name','Name','e.g. M. Adams'],['phone','Phone','078 000 0000'],['vehicle','Vehicle','Bakkie or truck'],['area','Area','Cape Town area']].map(([key, label, placeholder]) => <div key={key}><label className="mb-1.5 block text-xs font-bold text-slate-600">{label}</label><input required className="field" placeholder={placeholder} value={newDriver[key]} onChange={e => setNewDriver({ ...newDriver, [key]: e.target.value })} /></div>)}</div><button className="btn btn-primary mt-6 w-full">Add driver <Plus size={17} /></button></form></div>}</main>;
}
function Metric({ icon: Icon, label, value, detail, dark }) { return <div className={`rounded-2xl p-5 shadow-sm ${dark ? 'bg-brand text-white' : 'bg-white'}`}><div className="flex items-center justify-between"><p className={`text-xs font-bold uppercase tracking-wider ${dark ? 'text-blue-200' : 'text-slate-400'}`}>{label}</p><Icon size={18} className={dark ? 'text-blue-200' : 'text-brand'} /></div><p className="mt-4 text-3xl font-black">{value}</p><p className={`mt-1 text-xs ${dark ? 'text-blue-100' : 'text-slate-500'}`}>{detail}</p></div>; }
